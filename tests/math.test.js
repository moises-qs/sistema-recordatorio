import { describe, it, expect } from 'vitest'

describe('Prueba básica de matemáticas', () => {
  it('2 + 2 debería ser igual a 4', () => {
    expect(2 + 2).toBe(4)
  })

  it('5 * 3 debería ser 15', () => {
    expect(5 * 3).toBe(15)
  })
})
